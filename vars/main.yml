---
# vars file for bareos-client

### Default vars for bareos-client 
bareos_dir_hostname: 
bareos_mon_hostname: 
bareos_dir_password:
bareos_mon_password:
bareos_director:  # string
bareos_setup_dir: # boolean

### Default vars for backup on bareos fd 
bareos_file_backup: ['/etc', '/opt'] # this is a list
bareos_file_exclude: ['/tmp', '/var'] # this is a list
